<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mehrbod.a250movies">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FinderActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".RankResultActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".DirectorResultActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".ActorResultActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".MovieResultActivity"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>